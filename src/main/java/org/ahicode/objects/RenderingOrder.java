package org.ahicode.objects;

/**
 * BACKGROUND - for objects behind the player
 * MAIN - for objects on the same layer level as the player
 * FOREGROUND - for objects in front of the player
 */
public enum RenderingOrder {
    BACKGROUND,
    MAIN,
    FOREGROUND
}
